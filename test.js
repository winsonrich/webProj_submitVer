let x=1,y=2;
// Create an object:
const car = {type:"Fiat", model:"500", color:"white"};
//let {type,model}=car;

// Display some data from the object:
console.log("The car type is " + car.type+" <br>value of x is:"+x+" value of y is:"+y);